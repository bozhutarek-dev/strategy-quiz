# 项目交接说明（HANDOVER）

> 本文档面向接手本项目的开发者或 AI agent，读完即可独立维护和部署。

## 项目是什么

一个在线答题问卷：课前阅读材料《你肯定你有战略吗？》的 15 道单选题测试。
学员手机扫码作答 → 提交后自动批改（只显示得分，不显示答案）→ 成绩自动写回本仓库 `results/` 目录 → 组织者在看板查看统计或导出 Excel。

## 线上地址

- 学员端：https://bozhutarek-dev.github.io/strategy-quiz/
- 成绩看板：https://bozhutarek-dev.github.io/strategy-quiz/#admin （手动刷新，含分组统计、每题正确率）

## 技术架构

- 前端：React + TypeScript + Vite + Tailwind + shadcn/ui，纯静态，无后端
- 托管：GitHub Pages（`gh-pages` 分支，内容为 `dist/` 构建产物）
- 成绩存储：学员提交时，前端直接调用 GitHub Contents API，把成绩 JSON 写入本仓库 `main` 分支的 `results/` 目录（一人一文件）

## 关键文件

| 文件 | 作用 |
|------|------|
| `src/data/questions.ts` | 题库（15 题：题干、选项、correctIndex、解析）+ 页面标题 |
| `src/App.tsx` | 学员端（开始页：小组编号+姓名必填 → 答题 → 提交 → 只显示得分） |
| `src/Admin.tsx` | 成绩看板（`#admin` 路由，手动刷新） |
| `src/config.ts` | 成绩写回用的 GitHub 令牌（**编码拆分存放，切勿粘贴明文**，见下） |
| `deploy_via_api.py` | 部署脚本：通过 GitHub API 推送 main 源码 + gh-pages 产物并开启 Pages |
| `export_results.py` | 成绩导出：拉取 `results/` 全部记录生成 Excel（含小组、每题对错） |
| `generate_qr.py` | 二维码生成：`python3 generate_qr.py <URL> <输出.png>` |
| `docs/` | 过程文档归档：测试题整合版（15 题，含标准答案与原文引用）、参考答案公布页 HTML |

## 日常维护流程

```bash
npm install            # 首次克隆后安装依赖
# 修改题目：编辑 src/data/questions.ts
npm run build          # 构建到 dist/
python3 deploy_via_api.py   # 部署（推送 main + gh-pages，Pages 自动更新）
```

部署脚本通过 `gh auth token` 取认证，因此**运行环境需要已登录 GitHub CLI**
（`gh auth login`，账号 bozhutarek-dev，或任何对本仓库有写权限的账号）。
该脚本走 `api.github.com`，在 github.com 网页/443 被阻断的网络下也能工作；
网络正常时也可以用常规方式（`git push` + `npx gh-pages -d dist`）。
脚本用 base_tree 增量提交，**不会清除 `results/` 里的成绩数据**。

## 成绩提交令牌（重要）

- 类型：Fine-grained PAT，**仅授权 bozhutarek-dev/strategy-quiz 一个仓库、仅 Contents 读写**
- 存放：`src/config.ts` 中以"反转 + base64 + 拆分"形式保存，网页运行时还原
- **切勿把明文令牌提交进公开仓库——GitHub 会自动吊销检测到的明文令牌**（本项目踩过这个坑）
- 如令牌泄露或失效：在 GitHub → Settings → Developer settings → Fine-grained tokens 吊销/重建，
  新令牌按 `src/config.ts` 里同样的编码方式写入后重新部署

## 常见任务速查

- **改题目**：编辑 `src/data/questions.ts` → build → deploy
- **导出成绩**：`python3 export_results.py`（生成 `成绩汇总.xlsx`）
- **清空成绩**：删除仓库 `main` 分支 `results/` 下的 JSON 文件（Contents API DELETE）
- **重新生成二维码**：`python3 generate_qr.py https://bozhutarek-dev.github.io/strategy-quiz/ 问卷二维码.png`

## 已知事项

- 答案数据在客户端 bundle 中，技术上可被查看源代码的人提取；课前小测场景可接受
- 成绩存放在公开仓库，知道地址的人可见学员姓名和得分
- 学员端结果页只显示得分；看板 `#admin` 无密码保护，知道网址即可访问
